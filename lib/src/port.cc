
#include "port.h"
#include "kernel.h"

bool hamurai::Port::push(hamurai::DataItem const&item)
{
    /*
    // Enqueue the item...
    if( _dataQueue.enqueue( item ) )
    {
        // Notify the kernel of the new item
        return _parent->pushEvent( DataEvent(_ownName) );
    }
    else
        return false;
        */
}
